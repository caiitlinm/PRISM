# Verification pack — XAI-RMF-2025-02 p3 (Table A, header + rows 1–2)

Render: `XAI-RMF-2025-02-p3-3.jpg`. Confidence: `read` = unambiguous in the render,
`inferred` = reconstructed from partial evidence, `uncertain` = flag for you.

| Cell (row / col) | First six words | Conf. |
|---|---|---|
| Virology Capabilities Test (VCT)… / Benchmark | Virology Capabilities Test (VCT): vision–language questions | read |
| Virology Capabilities Test (VCT)… / Threshold | X% (e.g. 15%) | read |
| Virology Capabilities Test (VCT)… / Reference score | 22.1% by average expert virologists in | read |
| WMDP-Bio… / Benchmark | WMDP-Bio: multiple-choice questions on proxies of | read |
| WMDP-Bio… / Threshold | X% (e.g. 65%) | read |
| WMDP-Bio… / Reference score | 82.2% by GPT-4 (zero-shot) | read |

**All 6 cells `read`. Nothing inferred, nothing uncertain.**

## Watermark overlap, per cell

The DRAFT watermark runs diagonally from lower-left to upper-right. On page 3 the
letters `D` and `R` fall across the left and centre of the page.

| Cell | Watermark overlaps? | Affected my reading? |
|---|---|---|
| VCT / Benchmark | Yes — the `D` bowl crosses the lower-left of this cell | No. Watermark is pale grey at ~10% opacity; black body text is fully legible over it. |
| VCT / Threshold | No | — |
| VCT / Reference score | No | — |
| WMDP-Bio / Benchmark | Slight — lower edge of the `D` | No |
| WMDP-Bio / Threshold | No | — |
| WMDP-Bio / Reference score | No | — |

## Cross-check against the scrambled extraction

Performed after drafting, per instruction. Comparing against `XAI-RMF-2025-02.txt`
lines 156–195:

| Point | Draft (from render) | Scrambled extraction | Agree? |
|---|---|---|---|
| Header | Benchmark / Threshold / Reference score | `Benchmark    Threshold   Reference score` | ✅ |
| VCT reference score | 22.1% … 35.4% … January 2025 | present but split across lines, with `January 2025 (zero-shot` orphaned at line 171 | ✅ content agrees |
| WMDP-Bio row label | `WMDP-Bio: multiple-choice questions…` | `D    WMDP-Bio: multiple-choice` — stray watermark `D` prefixed | ⚠️ extraction corrupt, draft correct |
| WMDP-Bio reference score | 82.2% by GPT-4 (zero-shot) | present at line 181, but detached — sits three blank lines below the WMDP-Bio row with nothing tying it to that row | ⚠️ value survives, association does not |

**Disagreements to note:**

1. **The VCT reference score is split across the WMDP-Bio row.** Its final fragment
   `multiple-response)` lands at line 179 — *below* the WMDP-Bio row label at line
   175 — and `82.2% by GPT-4 (zero-shot)` then appears at line 181, below that. So
   reading the extraction top-down, the VCT cell's tail and the WMDP-Bio value are
   interleaved. Both values are present; both row associations are destroyed.
2. **Stray watermark `D`** prefixes the WMDP-Bio row label (`D    WMDP-Bio:`).

Neither changes the draft, which was made from the render. Recorded because the
failure mode is displacement, not deletion — an extraction that still contains every
number while attaching several of them to the wrong row.
