# Verification pack — XAI-RMF-2025-02 p5 (Table B, spans pp. 5–6)

Renders: `XAI-RMF-2025-02-p5-5.jpg` and `XAI-RMF-2025-02-p6-6.jpg`. Confidence:
`read` = unambiguous in the render, `inferred` = reconstructed from partial evidence,
`uncertain` = flag for you.

| Cell (row / col) | First six words | Conf. |
|---|---|---|
| Model Alignment… (MASK) / Benchmark | Model Alignment between Statements and Knowledge (MASK) | read |
| **Model Alignment… (MASK) / Thresholds** | **<X% lying on MASK (e.g. <50%)** | **read** |
| Utility Functions / Benchmark | Utility Functions | read |
| Utility Functions / Thresholds — item 1 | Utility/Power Correlation (Coercive): <W (e.g. | read |
| Utility Functions / Thresholds — item 2 | Utility/Power Correlation (Non-coercive): <X (e.g. | read |
| Utility Functions / Thresholds — item 3 | Utility/Fitness Correlation: <Y (e.g. <0.5) | read |
| Utility Functions / Thresholds — item 4 | Corrigibility Score: >Z (e.g. >0.2) | read |

**All cells `read`. Nothing inferred, nothing uncertain.** The four Thresholds items
are listed separately here for checking; in the draft they are one cell body, pending
your ruling on sub-structure.

## Watermark overlap, per cell

On page 5 the `A` and `F` of DRAFT cross the middle of the page; the table sits
low-left, below most of the watermark.

| Cell | Watermark overlaps? | Affected my reading? |
|---|---|---|
| MASK / Benchmark | Yes — the `D` bowl passes behind it | No. Pale grey; black text fully legible. |
| MASK / Thresholds | No | — |
| Utility Functions / Benchmark | No | — |
| Utility Functions / Thresholds (p5 portion) | No | — |
| Utility Functions / Thresholds (p6 portion) | No — page 6's watermark falls right of the table | — |

## Cross-check against the scrambled extraction

Performed after drafting. Against `XAI-RMF-2025-02.norm.txt` lines 300–330:

| Point | Draft (from render) | Scrambled extraction | Agree? |
|---|---|---|---|
| Header | Benchmark / Thresholds | `Benchmark    Thresholds` | ✅ |
| MASK row label | `Model Alignment between Statements and Knowledge (MASK)` | `DModel Alignment` — watermark `D` **fused to the word**, no space | ⚠️ extraction corrupt |
| **MASK / Thresholds** | **`<X% lying on MASK` + `(e.g. <50%)`** | appears at lines 310–311, **below the `Utility Functions` row label at line 308** | ⚠️ **draft and extraction disagree on the row** |
| Utility Functions / Thresholds | four metrics, first is Utility/Power Correlation (Coercive): <W | `Utility/Power Correlation (Coercive): <W` at line 316; `(e.g. <0)` marooned at line 325 after the footnote block and the page number `5` | ⚠️ cell split across a page break by footnotes |

**The disagreement that matters.** Reading the extraction top-down gives:

```
  Utility Functions
                                  <X% lying on MASK
                                   (e.g. <50%)
```

which attaches `<X% lying on MASK` to **Utility Functions**. The render shows
unambiguously that it belongs to **Model Alignment between Statements and Knowledge
(MASK)** — the row above. The row label and its threshold are on the same horizontal
band in the PDF; the extraction pushed the threshold down one row.

This is the single association the whole repair exists for, and the draft contradicts
the extraction. The draft is correct: the value reads `lying on MASK`, and MASK is the
benchmark named in that row.

**Second finding, not in the extraction report.** The Utility Functions Thresholds
cell is split by the page break *and* by the footnote block: `(e.g. <0)` for the first
metric appears at line 325, after footnotes 10 and 11 and the page number `5`. The
three remaining metrics (Non-coercive, Fitness, Corrigibility) follow on page 6. A
segmenter would read the footnote text as intervening cell content.
