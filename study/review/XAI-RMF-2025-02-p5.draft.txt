DRAFT rev.2 — NOT APPROVED. Do not splice into .norm.txt until verified.
Source: study/review/XAI-RMF-2025-02-p5-5.jpg and -p6-6.jpg (200 dpi renders)
Drafted from the renders only; scrambled extraction not consulted while drafting.

Revisions from rev.1, per your rulings:
- ROW-LABEL RULE (convention rule 1): the Benchmark column here IS a bare label —
  "Model Alignment between Statements and Knowledge (MASK)" and "Utility Functions"
  carry no prose beyond the name. DECISION FOR THIS TABLE: Benchmark blocks are
  DROPPED. This is the opposite call from Table A, and deliberately so; the two
  tables differ in what their first column holds.
- Utility Functions Thresholds cell now carries items="paragraphs". No "- " markers
  added — the document set these as paragraphs, and A1 will split them into four
  units on the blank lines.
- Table B spans pp. 5-6. Per convention rule 9 the cell split by the break is
  reassembled into one block and placed in the first table block, so the whole table
  is emitted here as a single block and page 6 needs no continuation block. Page 6 is
  outside the range you specified; rendering it was necessary because the Utility
  Functions cell is cut mid-value at "<W" by the break and resumes with "(e.g. <0)".

<<TABLE table_id="Table B" caption="Benchmark / Thresholds (loss of control)" page="5-6">>

<<CELL row="Model Alignment between Statements and Knowledge (MASK)" col="Thresholds">>
<X% lying on MASK
(e.g. <50%)
<</CELL>>

<<CELL row="Utility Functions" col="Thresholds" items="paragraphs">>
Utility/Power Correlation (Coercive): <W
(e.g. <0)

Utility/Power Correlation (Non-coercive): <X
(e.g. <0.5)

Utility/Fitness Correlation: <Y
(e.g. <0.5)

Corrigibility Score: >Z
(e.g. >0.2)
<</CELL>>

<</TABLE>>
